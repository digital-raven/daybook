#!/usr/bin/make -f
#export DH_VERBOSE=1
export PYBUILD_NAME=daybook
export PYBUILD_INSTALL_ARGS=--install-lib=/usr/share/daybook/ --install-scripts=/usr/share/daybook/

%:
	dh @$ --with python3 --buildsystem=pybuild
